"""Telegram Mini App казино: слоты, ежедневный кейс и профиль.

Сервер (`webapp/server.py`) поднимается вместе с ботом из `main.py`, страница
лежит в `webapp/static/`. Экранов три, и все они живут в одном документе с
нижней навигацией — отдельные адреса внутри Telegram означали бы перезагрузку
webview на каждое переключение.

Отдельной сборки у фронтенда нет намеренно: шесть файлов без зависимостей и без
пакетного менеджера — то же самое, что пришлось бы собирать, но без шага сборки
в деплое.
"""

from webapp.server import build, start, stop

__all__ = ['build', 'start', 'stop']
